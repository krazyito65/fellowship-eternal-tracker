[project]
name = "fellowship-tracker"
version = "0.1.0"
description = "Fellowship Eternal Tracker"
requires-python = ">=3.14"
dependencies = [
    "requests",
    "urllib3",
]

[tool.uv]
package = false
